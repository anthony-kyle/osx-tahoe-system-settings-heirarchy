# osx-tahoe-system-settings-heirarchy
A utility applescript for storing the heirarchy of an application window / pane on MacOSX to a JSON file. Default target is Display settings.

## Usage

Open your preferred terminal and run:
```bash
osascript osx-tahoe-system-settings-heirarchy.applescript
```
